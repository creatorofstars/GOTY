iseeall 增加溅射范围碰撞体显示（public/client.js）：

在现有黄色炮弹碰撞圆之外，再画一个**淡金色半透明圆**表示该炮弹的溅射（爆炸）范围：
- 半径按角色：轰侠(char 0) 77px（55×1.4），其余角色 55px；
- 样式：`rgba(255, 213, 74, 0.25)` 描边 + 极低透明度填充（0.05），与黄色本体碰撞圆同心；
- Boss 小兵炮弹不画溅射圈（落地只生成小兵，无爆炸）。

实现位置：现有 iseeall 的炮弹碰撞体绘制块内，紧跟黄色圆之后绘制；约 5 行改动。